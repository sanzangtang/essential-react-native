const initialState = {
  message: 'Hello, this message is from redux'
};

const reducer = (state = initialState, action) => {
  /* 
  handle your actions here
  */
  return state;
};

export default reducer;
